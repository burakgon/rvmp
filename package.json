{
  "name": "codegent",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "bun test",
    "dev:daemon": "bun run --cwd apps/daemon dev",
    "dev:web": "bun run --cwd apps/web dev"
  }
}
